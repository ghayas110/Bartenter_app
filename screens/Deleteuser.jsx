import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Deleteuser =({ onLogin ,onLogout}) => {
  return (
   <>
   </>
  )
}

export default Deleteuser

const styles = StyleSheet.create({})